import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    root: {
        display: "inherit",
        margin: "auto",
        marginTop: 25
    }
});

export default useStyles;
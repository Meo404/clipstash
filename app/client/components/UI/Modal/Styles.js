import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    closeModalButton: {
        marginTop: 25
    },
    modalPaper: {
        margin: 10
    },
});

export default useStyles;
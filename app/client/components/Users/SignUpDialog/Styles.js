import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    closeDialogButton: {
        marginTop: 25
    },
    signUpPaper: {
        margin: 10
    },
});

export default useStyles;